/**
 * DEPRECATED — hosting moved to Netlify (see NETLIFY_MIGRATION.md).
 * Previously synced production env between Vercel projects via `vercel env`.
 * Set variables in the Netlify dashboard (or `netlify env:set`) instead.
 */
console.error('DEPRECATED: Use Netlify Site settings → Environment variables.')
console.error('See NETLIFY_MIGRATION.md and server/docs/DEPLOYMENT.md.')
process.exit(1)
