import { PageShell } from '@/layouts/PageShell'

export function WorkPage() {
  return (
    <PageShell
      title="Work"
      path="/work"
      description="Selected projects, case studies, and delivery snapshots."
    />
  )
}
